import React from 'react'
import Header from './Header'
import { TravelPageContainer } from './StyleConfiguration'

const TravelPage = (props) => {
    return (
        <TravelPageContainer>
           <Header/>
        </TravelPageContainer>
    )
}

export default TravelPage
